SiteExport.py can be installed as an external method. It is
a copy of the SiteExport.py that comes with FileSystemSite, but
has support for exporting Formulator forms as well.

The license of SiteExport.py is the ZPL 2.0.

